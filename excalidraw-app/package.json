{
  "name": "excalidraw-app",
  "version": "1.0.0",
  "private": true,
  "homepage": ".",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "scripts": {
    "build": "vite build",
    "start": "vite",
    "serve": "npx http-server build -a localhost -p 5001 -o",
    "build:preview": "vite build && vite preview --port 5000"
  }
}
